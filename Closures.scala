
// So far, all function literals have referred only to passed parems, and not
// variables outside of that scope. When a function literal uses vars outside
// this initial scope, they are 'free variables' because the function literal
// does not itself give a meaning to them.
